import styled from 'styled-components';

export default styled.div`
  width: calc(100% + 10px);
  margin-left: -5px;
  margin-bottom: 150px;
  li {
    padding: 5px;
    img {
      width: 100%;
      height: auto;
      border-radius: 20px;
    }
  }
`;
